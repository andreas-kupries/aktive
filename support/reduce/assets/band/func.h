    // dst [count]         - destination row start - single band
    // src [count, stride] - source      row start - `stride` bands
    // count               - row width, source and destination
    // stride              - row depth, source only
